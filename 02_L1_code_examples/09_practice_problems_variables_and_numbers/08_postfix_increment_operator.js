let numerator = 10;
const DENOMINATOR = 2;
let answer = numerator / DENOMINATOR;
let incrementer = 1;
let end;
let difference;

incrementer++;
incrementer++;

console.log(`incrementer is ${incrementer}`); // => incrementer is 3